{
  "name": "xforge",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@10.0.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "test": "turbo test",
    "typecheck": "turbo typecheck",
    "gen": "turbo gen package",
    "gen:app": "turbo gen app",
    "gen:feature": "turbo gen feature",
    "lint": "pnpm lint:doctor && pnpm lint:biome && pnpm lint:architecture",
    "lint:architecture": "node tools/check-architecture-boundaries.mjs",
    "lint:biome": "biome check .",
    "lint:biome:ci": "biome ci .",
    "lint:doctor": "ultracite doctor",
    "lint:fix": "biome check --write .",
    "format": "biome format --write .",
    "check": "pnpm lint && pnpm typecheck && pnpm test",
    "clean": "turbo clean"
  },
  "devDependencies": {
    "@biomejs/biome": "catalog:",
    "@turbo/gen": "catalog:",
    "turbo": "catalog:",
    "typescript": "catalog:",
    "ultracite": "catalog:"
  }
}

import type { ReactElement } from "react";

import { TokenizePreview } from "../_components/tokenize-preview.tsx";

export default function TokenizePreviewPage(): ReactElement {
  return <TokenizePreview />;
}

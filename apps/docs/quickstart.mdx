---
title: Quickstart
description: Start the XForge workspace locally.
---

# Quickstart

1. Install dependencies with `pnpm install`.
2. Run the main app with `pnpm dev --filter app`.
3. Run the docs surface with `pnpm dev --filter docs`.

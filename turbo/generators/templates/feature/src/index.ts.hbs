/**
 * Server-only public door for the feature package.
 *
 * Client code must import pure subpaths such as `/contract`, `/manifest`, or
 * `/metadata` instead of the package root.
 */
import "server-only";

export type {
  Create{{ recordPascal }}Input,
  {{ recordPascal }}ActionName,
  {{ recordPascal }}Contract,
  {{ recordPascal }}ExecutionContext,
  {{ recordPascal }}Record,
  List{{ featurePascal }}Input,
  Update{{ recordPascal }}Input,
} from "./contract.js";
export {
  {{ recordCamel }}ActionNames,
  {{ recordCamel }}Contract,
} from "./contract.js";
export {
  create{{ recordPascal }}InputSchema,
  list{{ featurePascal }}InputSchema,
  update{{ recordPascal }}InputSchema,
  {{ recordCamel }}RecordSchema,
} from "./schema.js";
export type { {{ recordPascal }}Manifest } from "./manifest.js";
export { {{ recordCamel }}Manifest } from "./manifest.js";
export type { {{ recordPascal }}Metadata } from "./metadata.js";
export { {{ recordCamel }}Metadata } from "./metadata.js";
export type { {{ recordPascal }}Server } from "./server.js";
export {
  create{{ recordPascal }},
  {{ recordCamel }}Server,
  get{{ recordPascal }},
  list{{ featurePascal }},
  update{{ recordPascal }},
} from "./server.js";

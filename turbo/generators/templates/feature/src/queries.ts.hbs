import "server-only";

import type {
  {{ recordPascal }}ExecutionContext,
} from "./contract.js";
import type {
  {{ recordPascal }}Record,
  List{{ featurePascal }}Input,
} from "./schema.js";
import { assert{{ recordPascal }}TenantContext } from "./execution/index.js";

export async function list{{ featurePascal }}Records(
  context: {{ recordPascal }}ExecutionContext,
  _input: List{{ featurePascal }}Input = {},
): Promise<readonly {{ recordPascal }}Record[]> {
  assert{{ recordPascal }}TenantContext(context);

  throw new Error("{{ name }}.list is not implemented yet");
}

export async function get{{ recordPascal }}Record(
  context: {{ recordPascal }}ExecutionContext,
  id: string,
): Promise<{{ recordPascal }}Record | null> {
  assert{{ recordPascal }}TenantContext(context);

  if (!id) {
    throw new Error("Missing {{ name }} record id");
  }

  throw new Error("{{ name }}.getById is not implemented yet");
}

import type { ControlSize } from "../contracts/component-size.contract";

export {
  BUTTON_VARIANTS,
  type ButtonVariant,
  type ButtonVariantContract,
  CONTROL_SIZES,
  CONTROL_SIZES as BUTTON_SIZES,
} from "../contracts";

export type ButtonSize = ControlSize;

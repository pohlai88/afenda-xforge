services:
  meilisearch:
    image: getmeili/meilisearch:v1.15.2
    ports:
      - "7700:7700"
    environment:
      MEILI_ENV: development
      MEILI_MASTER_KEY: xforge-search-test-key
      MEILI_NO_ANALYTICS: "true"
    restart: unless-stopped
